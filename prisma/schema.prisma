generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("AGR_PG_URI")
}

model Tag {
	id	Int	@default(autoincrement())	@id
	search	String?
	comment	String?
}
