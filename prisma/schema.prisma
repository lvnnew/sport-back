generator client {
  provider = "prisma-client-js"
  previewFeatures = ["createMany", "groupBy"]
}

datasource db {
  provider = "postgresql"
  url      = env("AGR_PG_URI")
}

model Tag {
	id	Int	@default(autoincrement())	@id
	comment	String?
}
