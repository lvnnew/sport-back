
# DO NOT EDIT! THIS IS GENERATED FILE
global:
  env: noName
  clusterName: noName
  deployKind: noName
  imagePullPolicy: Always

dev: false

dockerRegistry: registry.gitlab.com/agro-sys

app:
  tag: :master
  replicas: 1

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: gitlab-registry

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: "80"

deployment:
  annotations:
    prometheus.io/path: '/metrics'
    prometheus.io/scrape: true
    prometheus.io/port: 2100

back:
  enabled: true

ingress:
  enabled: true

metrics:
  enabled: true

worker:
  enabled: false
  replicas: 1
  requests:
    memory: "128Mi"
    cpu: "0.15"
  limits:
    memory: "256Mi"
    cpu: "0.3"

workers:
  - name: emails
    file: dist/workers/emails/emailsWorker.js
  - name: general
    file: dist/workers/general/generalWorker.js

bot:
  enabled: false
  replicas: 1
  requests:
    memory: "128Mi"
    cpu: "0.15"
  limits:
    memory: "256Mi"
    cpu: "0.3"

bots:
  - name: hello
    file: dist/bots/helloBot.js

apollo:
  key: ''

database:
  uri: ''

ingress:
  annotations:
    nginx.ingress.kubernetes.io/limit-connections: "100"
    nginx.ingress.kubernetes.io/limit-rps: "300"
    nginx.ingress.kubernetes.io/limit-rpm: "5000"
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
  host: "apps.making.ventures"
  letsencryptCert: true
  domain: "agr-ep"

resources:
  {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
