
# DO NOT EDIT! THIS IS GENERATED FILE

{{- if .Values.worker.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: agr-general-worker
  labels:
    app: agr-general-worker
  annotations:
    prometheus.io/scrape: 'true'
spec:
  ports:
  - port: 3000
  selector:
    app: agr-general-worker
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agr-general-worker
spec:
  replicas: {{ .Values.app.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 3
  selector:
    matchLabels:
      app: agr-general-worker
  template:
    metadata:
      labels:
        app: agr-general-worker
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
      - name: agr-general-worker
        image: {{ .Values.dockerRegistry }}/agr-back{{ .Values.app.tag }}
        command: ["node", "dist/cli/generalWorker.js"]
        ports:
        - name: main-port
          containerPort: 3000
        imagePullPolicy: Always
        env:
        - name: RANDOM
          value: {{ .Values.random | quote }}
        - name: APP_NAME
          value: agr-general-worker
        - name: ENV
          value: '{{ .Values.global.env }}'
        - name: APOLLO_KEY
          value: {{ .Values.apollo.key | quote }}
        - name: AGR_PG_URI
          value: {{ .Values.database.uri | quote }}

        - name: AGR_APP_URI
          value: {{ .Values.app.prefix }}.{{ .Values.ingress.host }}

        - name: AGR_S3_ACCESS_KEY_ID
          value: {{ .Values.s3.accessKeyId | quote }}
        - name: AGR_S3_SECRET_ACCESS_KEY
          value: {{ .Values.s3.secretAccessKey | quote }}

        - name: AGR_SMTP_HOST
          value: {{ .Values.smtp.host | quote }}
        - name: AGR_SMTP_PORT
          value: {{ .Values.smtp.port | quote }}
        - name: AGR_SMTP_USER
          value: {{ .Values.smtp.user | quote }}
        - name: AGR_SMTP_PASS
          value: {{ .Values.smtp.pass | quote }}
        - name: AGR_SMTP_FROM
          value: {{ .Values.smtp.from | quote }}

        resources:
          requests:
            memory: "256Mi"
            cpu: "0.15"
          limits:
            memory: "512Mi"
            cpu: "1"

        securityContext:
          runAsNonRoot: true
          # readOnlyRootFilesystem: true
          runAsUser: 1000
---
{{- end }}