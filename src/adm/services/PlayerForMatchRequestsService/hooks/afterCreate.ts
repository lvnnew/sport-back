/* eslint-disable @typescript-eslint/no-empty-function */
import {
  PlayerForMatchRequest,
} from '../../../../generated/graphql';
import {Context} from '../../types';

export const afterCreate = async (
  _ctx: Context,
  _data: PlayerForMatchRequest,
): Promise<void> => {};
