/* eslint-disable @typescript-eslint/no-empty-function */
import {
  ConfigurationVariable,
} from '../../../../generated/graphql';
import {Context} from '../../types';

export const afterDelete = async (
  _ctx: Context,
  _data: ConfigurationVariable,
): Promise<void> => {};
