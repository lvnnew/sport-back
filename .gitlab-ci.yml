image: node:14

# DO NOT EDIT! THIS IS GENERATED FILE

stages:
  - check
  - build
  - latest-image
  - deploy-stage
  - deploy-prod

cache:
  paths:
    - .cache
    - .cache_images
    - node_modules

variables:
  REPO_NAME: $CI_PROJECT_NAME
  VERBOSE: 'true'
  RELEASE: $CI_COMMIT_SHORT_SHA
  KUBECONFIG: /etc/deploy/config
  APPNAME: back

check:
  stage: check
  image: registry.gitlab.com/making.ventures/images/prisma-cli
  before_script:
    - yarn
  script:
    - ./check

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"username\":\"$CI_REGISTRY_USER\",\"password\":\"$CI_REGISTRY_PASSWORD\"}}}" > /kaniko/.docker/config.json
    # - /kaniko/warmer --cache-dir=$CI_PROJECT_DIR/.cache_images --image=browserless/chrome
  script:
    - /kaniko/executor
      --cache-dir=$CI_PROJECT_DIR/.cache_images
      --context ${CI_PROJECT_DIR}
      --dockerfile ${CI_PROJECT_DIR}/Dockerfile
      --destination ${CI_REGISTRY_IMAGE}:$CI_COMMIT_REF_SLUG
      --single-snapshot
  only:
    - master
    - release
    - /^release-.*$/

master-to-latest:
  extends: .tag-image
  stage: latest-image
  only:
    - master
  variables:
    TAG_ORIGIN: master
    TAG_DESTINATION: latest

.tag-image:
  image:
    name: gcr.io/go-containerregistry/crane:debug
    entrypoint: ['']
  script:
    - crane auth login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - crane cp ${CI_REGISTRY_IMAGE}:${TAG_ORIGIN} ${CI_REGISTRY_IMAGE}:${TAG_DESTINATION}
  variables:
    GIT_STRATEGY: none

deploy-stage-back:
  extends: .deploy-dev-back
  stage: deploy-stage
  variables:
    ENV: "stage"
    ENV_NAME: "stage01"

deploy-prod-back:
  extends: .deploy-prod-back
  stage: deploy-prod
  variables:
    ENV: "prod"
    ENV_NAME: "prod01"


deploy-stage-workers:
  extends: .deploy-dev-workers
  stage: deploy-stage
  variables:
    ENV: "stage"
    ENV_NAME: "workers-stage-01"

deploy-prod-workers:
  extends: .deploy-prod-workers
  stage: deploy-prod
  variables:
    ENV: "prod"
    ENV_NAME: "workers-prod-01"


.deploy-dev-back:
  extends:
    - .deploy-dev
    - .deploy-back
  variables:
    KUBE_CONFIG: ${KUBE_STAGE01_CONFIG}
    BACK_ENABLED: "true"
    INGRESS_ENABLED: "true"
    METRICS_ENABLED: "true"
    WORKER_ENABLED: "false"
    BOT_ENABLED: "false"
    ROOT_ENABLED: "false"

.deploy-dev-workers:
  extends:
    - .deploy-dev
    - .deploy-worker
  variables:
    DEPLOY_KIND: "back"
    KUBE_CONFIG: ${KUBE_WORKERS01_CONFIG}
    APPNAME: workers
    BACK_ENABLED: "false"
    INGRESS_ENABLED: "false"
    METRICS_ENABLED: "false"
    WORKER_ENABLED: "true"
    BOT_ENABLED: "true"
    ROOT_ENABLED: "false"
  only:
    - master

.deploy-prod-back:
  extends:
    - .deploy-prod
    - .deploy-back
  variables:
    KUBE_CONFIG: ${KUBE_PROD01_CONFIG}
    BACK_ENABLED: "true"
    INGRESS_ENABLED: "true"
    METRICS_ENABLED: "true"
    WORKER_ENABLED: "false"
    BOT_ENABLED: "false"
    ROOT_ENABLED: "false"

.deploy-prod-workers:
  extends:
    - .deploy-prod
    - .deploy-worker
  variables:
    KUBE_CONFIG: ${KUBE_WORKERS01_CONFIG}
    APPNAME: workers
    BACK_ENABLED: "false"
    INGRESS_ENABLED: "false"
    METRICS_ENABLED: "false"
    WORKER_ENABLED: "true"
    BOT_ENABLED: "true"
    ROOT_ENABLED: "false"

.deploy-dev:
  extends: .deploy
  variables:
    DEV: "true"
    HOST: "making.ventures"
    TAG: ":latest"
    AGR_ADM_JWT_SECRET: ${AGR_DEV_ADM_JWT_SECRET}
    AGR_APP_JWT_SECRET: ${AGR_DEV_APP_JWT_SECRET}
    AGR_DATABASE_URI: ${AGR_DEV_DATABASE_URI}
    AGR_S3_ACCESS_KEY_ID: ${AGR_DEV_S3_ACCESS_KEY_ID}
    AGR_S3_SECRET_ACCESS_KEY: ${AGR_DEV_S3_SECRET_ACCESS_KEY}
    AGR_SMTP_FROM: ${AGR_DEV_SMTP_FROM}
    AGR_SMTP_HOST: ${AGR_DEV_SMTP_HOST}
    AGR_SMTP_PASS: ${AGR_DEV_SMTP_PASS}
    AGR_SMTP_PORT: ${AGR_DEV_SMTP_PORT}
    AGR_SMTP_USER: ${AGR_DEV_SMTP_USER}
  only:
    - master

.deploy-prod:
  extends: .deploy
  stage: deploy-prod
  when: manual
  variables:
    ENV: "prod"
    DEV: "false"
    HOST: "making.ventures"
    TAG: ":latest"
    AGR_ADM_JWT_SECRET: ${AGR_PROD_ADM_JWT_SECRET}
    AGR_APP_JWT_SECRET: ${AGR_PROD_APP_JWT_SECRET}
    AGR_DATABASE_URI: ${AGR_PROD_DATABASE_URI}
    AGR_S3_ACCESS_KEY_ID: ${AGR_PROD_S3_ACCESS_KEY_ID}
    AGR_S3_SECRET_ACCESS_KEY: ${AGR_PROD_S3_SECRET_ACCESS_KEY}
    AGR_SMTP_FROM: ${AGR_PROD_SMTP_FROM}
    AGR_SMTP_HOST: ${AGR_PROD_SMTP_HOST}
    AGR_SMTP_PASS: ${AGR_PROD_SMTP_PASS}
    AGR_SMTP_PORT: ${AGR_PROD_SMTP_PORT}
    AGR_SMTP_USER: ${AGR_PROD_SMTP_USER}
  only:
    - master

.deploy-back:
  variables:
    DEPLOY_KIND: "back"

.deploy-worker:
  variables:
    DEPLOY_KIND: "worker"

.deploy-telegramBot:
  variables:
    DEPLOY_KIND: "telegramBot"

.deploy:
  image:
    name: alpine/helm:3.1.2
    entrypoint: [""]
  before_script:
    - mkdir -p /etc/deploy
    - echo ${KUBE_CONFIG} | base64 -d > ${KUBECONFIG}
  script:
    - NAMESPACE=${NAMESPACE:-"agr-${ENV}"}
    - echo NAMESPACE ${NAMESPACE}

    - helm upgrade
      --install
      --wait ${NAMESPACE}-${APPNAME} chart
      --timeout 3600s
      --set "global.envName=${ENV_NAME}"
      --set "global.env=${ENV}"
      --set "global.deployKind=${DEPLOY_KIND}"
      --set "random=:$(date)"
      --set "app.tag=${TAG}"
      --set "ingress.host=${HOST}"
      --set "style=${STYLE}"
      --set "back.enabled=${BACK_ENABLED}"
      --set "ingress.enabled=${INGRESS_ENABLED}"
      --set "metrics.enabled=${METRICS_ENABLED}"
      --set "worker.enabled=${WORKER_ENABLED}"
      --set "bot.enabled=${BOT_ENABLED}"
      --set "ingress.rootEnabled=${ROOT_ENABLED}"
      --set "adm.jwt.secret=${AGR_ADM_JWT_SECRET}"
      --set "app.jwt.secret=${AGR_APP_JWT_SECRET}"
      --set "database.uri=${AGR_DATABASE_URI}"
      --set "s3.accessKeyId=${AGR_S3_ACCESS_KEY_ID}"
      --set "s3.secretAccessKey=${AGR_S3_SECRET_ACCESS_KEY}"
      --set "smtp.from=${AGR_SMTP_FROM}"
      --set "smtp.host=${AGR_SMTP_HOST}"
      --set "smtp.pass=${AGR_SMTP_PASS}"
      --set "smtp.port=${AGR_SMTP_PORT}"
      --set "smtp.user=${AGR_SMTP_USER}"
      --namespace ${NAMESPACE}
